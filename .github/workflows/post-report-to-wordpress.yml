name: Post report.md to WordPress

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print report.md
        run: |
          echo "---- REPORT CONTENT ----"
          cat report.md
          echo "------------------------"
