name: Post report.md to WordPress

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check report.md exists
        run: |
          ls -la
          test -f report.md && echo "report.md megtalálva"
